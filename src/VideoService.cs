using System;
using Raylib_cs;

namespace generalNamespace;

public class VideoService
{
     public void draw()
    {
        Raylib.BeginDrawing();
         Raylib.ClearBackground(Color.BLACK);

            Raylib.EndDrawing();
        
    }
}